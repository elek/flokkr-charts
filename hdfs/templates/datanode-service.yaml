apiVersion: v1
kind: Service
metadata:
  name: {{ template "hdfs.fullname" . }}-datanode
  labels:
    app: {{ template "hdfs.fullname" . }}-datanode
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - port: 9874
    name: web
  clusterIP: None
  selector:
     app: {{ template "hdfs.fullname" . }}-datanode
