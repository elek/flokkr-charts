Apache Hadoop up and running. Please use port forwarding to access the pod ports:


export NAMENODE_POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ template "hdfs.name" . }}-scm,release={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
kubectl port-forward $NAMENODE_POD_NAME 9876:9876

{{- if .Values.ingress.enabled }}
You can also access the cluster oger
{{- range .Values.ingress.hosts }}
  http://{{ . }}/namenode
{{- end }}
{{- end }}
