Apache Hadoop HDFS-7240 up and running. Please use port forwarding to access the pod ports:


export SCM_POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ template "ozone.name" . }}-scm,release={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
kubectl port-forward $SCM_POD_NAME 9876:9876

